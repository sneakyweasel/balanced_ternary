from research.open_problems.definition import ProblemDefinition

PROBLEM = ProblemDefinition(
    id="primes",
    title="Balanced-ternary sparse primes",
    status="EXPLORATORY",
    statement=(
        "Placeholder for prime / balanced-ternary investigations. Currently "
        "exposes only the existing sparse-prime enumerator."
    ),
    bt_relevance="Weight is computed in the canonical balanced-ternary expansion.",
    docs=("docs/problems/primes.md",),
)
