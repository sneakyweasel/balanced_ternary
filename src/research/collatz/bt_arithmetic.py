"""Compatibility shim. Canonical implementation: :mod:`bt.arithmetic`."""

from bt.arithmetic import (
    add,
    add_one,
    lsd_add_one_case,
    multiply_by_three,
    three_n_plus_one_word,
)
from bt.normalization import rewrite_sum as _rewrite_sum
