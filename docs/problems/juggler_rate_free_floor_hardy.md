# Juggler rate-free floor-Hardy equidistribution (the composition is the door)

Status: **CLOSE** (the named question is answered: there is no
genuinely rate-free published route through
\(\lfloor n^{3/2}\rfloor^{9/4}\); the floor-Hardy composition is
the irreducible door. The \(\beta>\beta_*\) fallback is a finite
Weyl family of that same composition, not a weaker species.
Rate-free conjecture stays ACTIVE.)

Successor of
[juggler_v94_rate_free](juggler_v94_rate_free.md) (**CLOSE**),
[juggler_v94_hardy_lift](juggler_v94_hardy_lift.md) (**CLOSE**),
and [juggler_nil_pet_reentry](juggler_nil_pet_reentry.md)
(PET as a method **CLOSE**), taking up the three questions those
records left open: a broader-than-three-name literature audit;
whether the node-wise \(\beta>\beta_*\) fallback of
`J-rate-free-density-one` reduces to a known rate-free class;
and whether a characteristic-factor-free Weyl / unique-ergodicity
principle treats the floor intrinsically. Answer: **no, no, and
no.** Not an equidistribution theorem, not a \(K_3\) bound, not
a Paper B edit, and not a reopen of BB/GG/JJ, Theorem R,
\(\lambda=0\), PET, state packing, flight DK, or the odd-tower
branch.

## Problem

Whether there is a genuinely rate-free mathematical route through
the floor-Hardy composition \(\lfloor n^{3/2}\rfloor^{9/4}\)
sufficient for a node-wise E-share \(\beta>\beta_*=1-\log 2/\log 3\),
or whether that composition is the irreducible door.

## Exact statement

Let \(v=\lfloor n^{3/2}\rfloor\). After
`J-tower-heisenberg-coordinate` and
`J-heisenberg-vertical-riemann`, the rate-free tower target
(`juggler_tower_rate_free_equidistribution`) reduces to
identification of the floor-Hardy orbit, and the only axis that
is not a reduction lemma is \(\{v^{9/4}\}\) together with the
horizontal triple
\(\bigl(\tfrac32 v^{3/4},\,v^{3/2},\,\tfrac12 v^{9/4}\bigr)\).
The vertical Mal'cev cut is already Riemann-integrable modulo a
Haar-null face; regularity is not reopened.

**1. Published theorems miss the exact composition (KNOWN;
no new ledger row).** For each candidate, the hypothesis is
checked against \(\lfloor n^{3/2}\rfloor^{9/4}\), not against a
Taylor expansion of that expression.

| Source | Hypothesis | Instance |
|---|---|---|
| Boshernitzan 1994 (`boshernitzan-1994-hardy-fields`) | \(f\) Hardy in \(n\), polynomial growth, log-away from \(\mathbb Q[t]\); then \(\{f(n)\}\) equidistributes | \(\{n^{27/8}\}\) matches; \(\lfloor n^{3/2}\rfloor^{9/4}\) is not Hardy in \(n\) |
| Frantzikinakis 2009 (`frantzikinakis-2009-sparse-nilmanifolds`) | floor sits in the *time slot* of a *fixed* nilrotation \(b^{\lfloor a(n)\rfloor}x\) | not a Hardy monomial of a floor; not \(g(v)=\exp\bigl(\tfrac32 v^{3/4}e_{12}+v^{3/2}e_{23}\bigr)\) |
| Richter 2023 (`richter-2023-hardy-nilmanifolds`) | \(a_1^{f_1(n)}\cdots a_k^{f_k(n)}\Gamma\) with each \(f_i\) Hardy of polynomial growth, property (P) | smooth model \((n^{3/4},n^{3/2},n^{9/4})\) matches; \(\lfloor n^{3/2}\rfloor^{9/4}\) does not |
| Tsinas 2023 (`tsinas-2023-pointwise-hardy-nilmanifolds`) | \(b_i^{a_i(n)}\Gamma\) with each \(a_i\) Hardy of polynomial growth | same time-slot as Richter; Host–Kra is applied *after* that identification |
| Bergelson–Leibman 2007 (`bergelson-leibman-2007-generalized-polynomials`) | generalized polynomials generated by *ordinary* polynomials, \(+\), \(\times\), and floor | \(n^{3/2}\) is not a polynomial; \(\lfloor n^{3/2}\rfloor^{9/4}\) is not a GP |
| Fejér / Kuipers–Niederreiter (`kuipers-niederreiter-1974-uniform-distribution`) | \(f'\) monotone and \(f'\to\pm\infty\) | the floor interpolant has \(F'=0\) a.e.; the smooth interpolant \(n^{27/8}\) has leftover \(\tfrac94 n^{15/8}\theta\), not \(o(1)\) |
| Host–Kra 2005 (`host-kra-2005-nilmanifolds`) | PET / characteristic factors on nilsequences | first difference re-expands \(A\{\Delta B\}\) of GG species (`J-nil-pet-reentry`); not re-tested |
| Joint ergodicity of Hardy iterates | \(T^{\lfloor a_i(n)\rfloor}\) (floor in the time slot) | same miss as Frantzikinakis |
| Digital PS (Mauduit–Rivat, Morgenbesser, Müllner–Spiegelhofer) | outer function digital / \(q\)-additive | outer function here is a second convex power (Paper B §2) |
| Beatty composition | inner floor error bounded | \(n^{3/2}\) is not linear |
| Kolesnik / arXiv 2510.20562 | rated van der Corput; twice-iterated PS at *small* amplitudes | rated, and \(A'\asymp n^{7/8}\gg 1\); not a rate-free door |
| Leibman / Green–Tao–Ziegler | polynomial (or polynomial-orbit) sequences | the path is Hardy-of-floor, not polynomial |
| Qualitative van der Corput (keep-\(v\)) | \(\{\Delta v^{9/4}\}=\{\Delta v\cdot\{\tfrac94\xi^{5/4}\}\}\) | integer dilation of \(\{v^{5/4}\}\), the same unbuilt composition ([juggler_v94_rate_free](juggler_v94_rate_free.md)) |

Taylor-expanding \(\lfloor n^{3/2}\rfloor^{9/4}\) into Hardy
terms is **not** a citation of any row above. The three-term
identity is Lemma G ([juggler_v94_hardy_lift](juggler_v94_hardy_lift.md)).

**2. Minimal missing theorem (UNBUILT_DOOR).** Full Haar
equidistribution is stronger than termination needs. After an
odd landing \(x\), the next letter is E if and only if
\(\lfloor x^{3/2}\rfloor\) is even, if and only if
\(\{x^{3/2}/2\}\in[0,1/2)\). Lemma B of
`J-rate-free-density-one` therefore asks only for a node-wise
liminf of that half-interval mass strictly above
\(\beta_*=1-\log 2/\log 3\).

The half-interval indicator is Riemann integrable. For every
\(\beta\in(\beta_*,1/2]\) there is a continuous tent
\(0\le\psi\le 1_{[0,1/2)}\) with \(\int\psi>\beta\) (the tent
that rises on \([0,\varepsilon]\), stays \(1\) on
\([\varepsilon,1/2-\varepsilon]\), and falls on
\([1/2-\varepsilon,1/2]\), has integral \(\tfrac12-\varepsilon\);
\(\varepsilon=0.12\) gives \(0.38>\beta_*\)). A trigonometric
polynomial of some finite degree \(K=K(\beta)\) approximates
\(\psi\) uniformly, so Weyl's criterion on **finitely many
fixed harmonics** of the single phase \(\{x^{3/2}\}\) implies
the \(\beta\)-fallback.

This is a complexity reduction, not a species reduction. At the
first unproved node the phase is a coordinate of the floor-Hardy
tower. Each harmonic \(e(k v^{9/4})\) with \(k\) fixed and
nonzero still has derivative \(\asymp n^{7/8}\gg 1\)
(`J-horizontal-axis-species`); finite \(K\) does not put the
sums into Theorem R, Boshernitzan, Richter, or Frantzikinakis.

The weakest unbuilt statement that would finish the route is
therefore: for each fixed surviving node, the Weyl averages of
\(\{x^{3/2}\}\) through degree \(K(\beta)\) vanish. A convenient
stronger form, still unbuilt, is equidistribution of
\(n\mapsto\{\lfloor n^{3/2}\rfloor^{9/4}\}\). The missing
published lemma that would give the stronger form is Leibman /
Richter for the composition \(a^{f(\lfloor h(n)\rfloor)}\) with
\(f\) a *nonlinear* Hardy monomial (the circle case:
\(\{f(\lfloor h(n)\rfloor)\}\) equidistributes whenever
\(\{f(h(n))\}\) does and \(f'\circ h\not\to 0\)).

**3. Direct floor treatment recreates \(n^{15/8}\theta\)
(KNOWN; corollary of `J-horizontal-axis-species`).** Mean
value: for \(\alpha>0\), \(X=n^{3/2}\), \(v=\lfloor X\rfloor\),
\(\theta=\{X\}\),
\[
\bigl\lvert v^\alpha-X^\alpha\bigr\rvert
=\alpha\,\xi^{\alpha-1}\theta
=O\bigl(X^{\alpha-1}\bigr),
\qquad\xi\in(v,X).
\]
The defect is \(o(1)\) if and only if \(\alpha<1\). For
\(\alpha=9/4\) it is \(\tfrac94 n^{15/8}\theta+O(n^{3/8})\),
already HH, forbidden as a proof route by
`J-nested-floor-without-W-family`. Unwinding through \(\theta\)
produces no new information.

Keeping \(v\) as an integer is the intrinsic reading: the
sequence is \(\{m^{9/4}\}\) along the Piatetski–Shapiro image
\(m=v(n)\). Boshernitzan applies to \(\{m^{9/4}\}\) as
\(m\) runs through \(\mathbb N\). The image has gaps
\(\asymp m^{1/3}\) and density zero. Rate-free unique
ergodicity of the full-integer sequence does not pass to a
zero-density subsequence. The published exception is the
*linear* path \(b^{\lfloor a(n)\rfloor}\) (Frantzikinakis).
A nonlinear Hardy path sampled at PS times is exactly
\(a^{f(\lfloor h(n)\rfloor)}\).

**4. No characteristic-factor-free unique-ergodicity principle
applies (KNOWN).** Weyl's criterion on the horizontal torus is
characteristic-factor-free and is the laboratory reduction
already recorded. It asks for the same Weyl sums. Unique
ergodicity of a *transformation* does not apply: the map
\(n\mapsto\bigl(\tfrac32 v^{3/4},v^{3/2},\tfrac12 v^{9/4}\bigr)\)
is a non-autonomous path, not an orbit of a fixed nilrotation.
Unique ergodicity of a *Hardy sequence* is Boshernitzan /
Richter and requires Hardy-in-\(n\) entries. There is no third
published principle. Reproducing \(A\{\Delta B\}\) is the
already-closed PET route and is not taken.

**5. Horizontal versus vertical.** The vertical is
Riemann-integrable (`J-heisenberg-vertical-riemann`); Haar
identification of the orbit would pass to it. The unresolved
issue is equidistribution of the horizontal triple, i.e. the
abelian axis \(\{v^{9/4}\}\). That is the door of (1)–(4).

## Current literature

Project relationship: **known** (every cited theorem) /
**reproduced** (leftover, finite-Weyl reduction, PET miss).

- The three-name door record
  [juggler_v94_rate_free](juggler_v94_rate_free.md).
- Lemma G packaging
  [juggler_v94_hardy_lift](juggler_v94_hardy_lift.md).
- PET re-entry `J-nil-pet-reentry`.
- Paper B §2: no published result for
  \(\{f(\lfloor g(n)\rfloor)\}\) with \(f,g\) convex
  non-polynomial powers.
- New registry rows: `bergelson-leibman-2007-generalized-polynomials`,
  `tsinas-2023-pointwise-hardy-nilmanifolds`.

## Branch budget

```text
Mathematical target     is there a genuinely rate-free route through
                        floor(n^{3/2})^{9/4}, or is the floor-Hardy
                        composition the irreducible door?
Novelty hypothesis      a published theorem missed by the 3-name
                        audit covers the exact composition; or the
                        node-wise β>β* fallback reduces to a finite
                        Weyl family already in a known rate-free
                        class; or the floor can be treated
                        intrinsically without recreating n^{15/8}θ
Falsifier               every candidate theorem fails the exact
                        composition; floor-intrinsic treatment
                        recreates GG/HH or is Hardy-along-PS
                        without a transfer theorem; the β-reduction
                        stays the same unbuilt composition
Existing machinery      J-rate-free-density-one, Heisenberg lift,
                        v94 door CLOSE, PET CLOSE, Lemma G,
                        J-horizontal-axis-species
Maximum Phase-0 scope   literature mismatch table beyond the
                        3-name audit; finite-observable reduction
                        of the β-fallback; unwind/intrinsic floor
                        check; no census, no Lean, no Paper B
Promotion criterion     a verbatim citation, an exact reduction to
                        a known rate-free class, a new β>β*
                        theorem, or an intermediate theorem that
                        removes the composition obstacle
Stop criterion          surviving statements are KNOWN,
                        REPARAMETERIZATION, or UNBUILT_DOOR
```

## Balanced-ternary formulation

None required. The objects live on \(\mathbb T\) and on
\(H_3(\mathbb R)/H_3(\mathbb Z)\).

## Why BT may be relevant

It is not required.

## Candidate operations / invariants

- Hypothesis match against \(\lfloor n^{3/2}\rfloor^{9/4}\) —
  **KNOWN** (table above; no new ledger row)
- Next-letter identity
  \(\lfloor x^{3/2}\rfloor\) even \(\iff\{x^{3/2}/2\}\in[0,1/2)\)
  — **EXACT — HUMAN PROOF** (elementary:
  \(\lfloor\alpha\rfloor=2\lfloor\alpha/2\rfloor+\lfloor 2\{\alpha/2\}\rfloor\))
- Finite-harmonic reduction of the \(\beta\)-fallback —
  **KNOWN** (Fejér / uniform approximation of a tent;
  Weyl's criterion). **Not** a species change
- Taylor transfer threshold \(\alpha<1\) —
  **KNOWN** (mean-value; already
  `J-horizontal-axis-species` at \(\alpha=9/4\))
- Hardy-along-PS transfer from Boshernitzan on \(\mathbb N\) —
  fails; density-zero sampling is not free
- Equidistribution of \(\{v^{9/4}\}\) — not claimed
- A second PET step — not taken

## Experiments

None as a research module. The parity identity, the tent
integral \(\tfrac12-\varepsilon>\beta_*\), and the exponent
\(\tfrac32(\tfrac94-1)=\tfrac{15}{8}>0\) are checked in
`tests/research/juggler_sequence/test_rate_free_floor_hardy.py`.
No Weyl grid: the nil-lift already has \(124\) harmonics at
\(\sqrt N\).

## Conjectures

None new. `juggler_tower_rate_free_equidistribution` stays
**ACTIVE** as the unbuilt composition
\(a^{f(\lfloor h(n)\rfloor)}\). Conjectures V/HH stay PARKED.

## Counterexamples

None. The novelty hypothesis died by obstruction (hypothesis
mismatch and same-species reduction), not by a counterexample
orbit. No orbit is claimed to fail equidistribution.

## Formalization

None. Lean-ifying a literature mismatch or a Fejér tent ahead
of an equidistribution theorem that would consume them is
machinery gravity.

## Results

Classification **RATE_FREE_FLOOR_HARDY_DOOR_UNBUILT**.

- **Published doors miss**, including the names the 3-name
  audit did not list: Tsinas 2023, Bergelson–Leibman 2007,
  Fejér, joint-ergodicity Hardy iterates, digital PS, Beatty
  composition, Kolesnik / twice-iterated PS, and polynomial
  nil-orbits. No new ledger row.
- **The \(\beta\)-fallback is a finite Weyl family of the
  same composition.** It does not open a published door.
- **Unwinding through \(\theta\) recreates
  \(n^{15/8}\theta\)**, not \(o(1)\). Intrinsic floor
  treatment is Hardy-along-PS, which has no rate-free
  transfer theorem for a nonlinear path.
- **Characteristic-factor-free Weyl is the horizontal
  identification**, already the laboratory reduction. There
  is no autonomous unique-ergodic map whose orbit is the
  floor-Hardy triple.
- Not claimed: equidistribution; density-one; a \(K_3\)
  bound; that the rate-free conjecture is false.

## Open questions

- Equidistribution of the floor-Hardy nil-orbit — still the
  active conjecture; the missing lemma is Hardy-of-floor
  composition for nil-orbits (Leibman / Richter on
  \(a^{f(\lfloor h(n)\rfloor)}\), \(f\) nonlinear). External;
  not opened as a laboratory branch.
- The \(\beta>\beta_*\) fallback is now identified as a
  finite Weyl family of that same composition and is not a
  separate laboratory door.

## Decision

**CLOSE.** The boxed question is answered: there is no
genuinely rate-free mathematical route through
\(\lfloor n^{3/2}\rfloor^{9/4}\) in the published theory, and
the floor-Hardy composition itself is the irreducible door.
The \(\beta>\beta_*\) fallback does not weaken the species.
The rate-free conjecture stays ACTIVE as that external
composition problem. Best next question: none from this door;
the live target remains
`juggler_tower_rate_free_equidistribution` as
\(a^{f(\lfloor h(n)\rfloor)}\), and no closed route is
reopened.

## Publication assessment

Status: `ARCHIVED`. A placement record that names the
irreducible composition and records that the bias fallback
is the same door. Not a paper claim. No Paper B edit.
