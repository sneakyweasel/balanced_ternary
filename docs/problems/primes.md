# Primes

Status: **EXPLORATORY**

Placeholder for prime / balanced-ternary investigations. Currently only the
existing sparse-prime enumerator is registered. Trial `is_prime` is an
inspection helper, not a primality library. Residual complexity of
primality is a separate closed branch:
[prime_residual_complexity.md](prime_residual_complexity.md).

## Branch budget

Not written. This is a placeholder, not an open branch. Any budget here
must first state what a balanced-ternary digit condition could say about
primes that residue conditions modulo \(3^k\) do not.

## Decision

`PARK`. Nothing is being investigated; the enumerator exists so that a
future branch is cheap to start. Do not grow the module without a
budget, and do not turn the trial helper into a primality library.

Best next question: does any balanced-ternary digit statistic separate
primes from a matched set of odd integers?

## Publication assessment

Status: `EXPLORATORY`. No result is claimed.
