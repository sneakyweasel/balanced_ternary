# Operator dynamics

Status: **EXPLORATORY**

Composition censuses and sequence dossiers. Operator definitions remain in
`bt.operators`. See [operator_algebra.md](../operator_algebra.md).

## Branch budget

Not written. The censuses predate the [methodology](../methodology.md).
A budget must state which composition question the census answers,
because a larger census is not itself a result.

## Decision

`PARK`. The operator identities and refutations that came out of this
line (`W` is not an involution, \(W\circ T \ne T\circ W\)) are already
on the ledger and in `conjectures/refuted/`. The remaining activity is
taxonomy, which is a stop signal rather than a reason to continue.

Best next question: is there a composition identity in the
\(\{S,N,D,W\}\) fragment that is not a consequence of the recorded ones?

## Publication assessment

Status: `EXPLORATORY`. The exact operator facts live on the ledger; the
censuses are supporting data.
