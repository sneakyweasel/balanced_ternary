# Juggler length-7 passenger slot

Status: **PARK**. Theorem X and density \(57/64\) stay
**CONJECTURE**. The slogan that Theorem T applies as a
passenger is **REFUTED**. Paper B stays frozen at \(13/16\).

Phase-34 desk classification of the length-7 sixth-letter
modes against the Phase-26/27 slots. Child of
[juggler_engine_harvest.md](juggler_engine_harvest.md). Not
a \(K_3\) attack and not a Paper B edit.

## Problem

After the \(\alpha=33/32\) \(W\)-family instance and the
remainder engine, does the leftover length-7 inventory sit
in Lemma 5.2 Stage 2 / Theorem T’s existing budgets, as
the Phase-13 draft claimed?

## Exact statement

Are the \(\theta_p\) modes of Lemma X1 and the Phase-29
frequencies \(e(un^{9/8})\) Lemma 5.2 Stage-2 \(X\)-modes
with \(\lvert u\rvert\le P^{1/4}\), (D1) decorations with
\(\lvert q'\rvert\le P^{1/16}\), or (D3) phases with
\(\lvert\varphi''\rvert\le 3kh_1h_2P^{-5/8}\)?

## Current literature

- Theorem T / Part XII (`J-depth5-contracting`): Stage-2
  slot \(R_0=P^{1/4}\) for \(e(uX)\) —
  **EXACT — HUMAN PROOF**. **reproduced**.
- Lemma X4 (`J-length7-remainder-engine`): isolated
  remainder sum — **EXACT — HUMAN PROOF**. **independent**.
- Theorem R at \(\alpha=33/32\) — **EXACT — HUMAN PROOF**.
  **independent** (that piece sits).
- Theorem X (`J-depth7-engine-contracting`) —
  **CONJECTURE**. **extended** by a named slot failure.
- Length-5 wrong-slot slogan (Phase 26, repaired Phase 27)
  — the same pattern. **reproduced**.

## Branch budget

```text
Mathematical target     Do the sixth-letter X-modes and the
                        n^{9/8}-frequencies sit in Lemma 5.2
                        / Theorem T's existing budgets?
Novelty hypothesis      After R′ at 33/32 and the remainder
                        engine, the leftover is the Phase-27
                        passenger class.
Falsifier               A displayed Stage-2 / (D1) / (D3)
                        margin that dies at |B| ≍ P^{27/32}
                        or |u| ≍ P^{31/96}.
Existing machinery      Part XII; Lemma 5.2; Lemmas X1, X4.
Maximum Phase-0 scope   Desk classification of the two named
                        species. No vdC III, no length-8,
                        no Paper B, no new CLI.
Promotion criterion     Both species land in a named existing
                        slot with displayed room.
Stop criterion          A load-bearing margin dies (PARK X;
                        name the hole). Machinery gravity.
```

## Balanced-ternary formulation

None required. The map is on ordinary positive integers.

## Why BT may be relevant

It is not required. The 2-adic / BT bridge stays closed.

## Candidate operations / invariants

- \(\theta_p\) coefficient \(B\asymp kn^{27/32}\), argument
  \(n^{27/16}\) — **EXACT — HUMAN PROOF**
- Remainder frequencies \(\lvert u\rvert\le P^{31/96}\),
  argument \(n^{9/8}\) — **EXACT — HUMAN PROOF**
- First-letter chirp \(C\asymp kn^{33/32}\) —
  **EXACT — HUMAN PROOF**
- “Theorem T applies as a passenger”
  (`J-length7-passenger-theorem-t`) — **REFUTED**
- Theorem X / density \(57/64\) — **CONJECTURE**
- Length-8 \(E'\) — **CONJECTURE** (untouched)
- \(K_3\) bound — **PARK** (BB/GG/JJ)

## Experiments

No new runner. Exponent seals in
`tests/research/juggler_sequence/test_two_step_parity.py`
(`test_length7_passenger_slots`).

## Conjectures

None new. `J-depth7-engine-contracting` and
`J-seven-step-descent-density` stay conjectures.

## Counterexamples

None for the counts. The Phase-13 passenger citation is
the counterexample to the *method*.

## Formalization

None added.

## Results

- **Proposition X-pass (REFUTED,
  `J-length7-passenger-theorem-t`).**
  The sixth letter is not a Theorem T / Stage-2 passenger.
  Proof: lemma Part XV.

## Open questions

Answered in Phase 35
([juggler_length7_vdc3.md](juggler_length7_vdc3.md)):
the isolated monomials close; the reduction is not a
decoration. Remaining: \(e(uw^{3/2})\) on X3-runs plus
the Q/R3 carry. Length 8 stays behind \(E'\).

## Decision

**PARK** Theorem X. The existing-budget route is closed:
\(\theta_p\) is an \(n^{27/16}\)-chirp at
\(\lvert u\rvert\asymp P^{27/32}\), and the remainder
modes are \(n^{9/8}\)-engines at \(\lvert u\rvert\asymp
P^{31/96}\). Lemma 3.3 is worse than trivial on both
chirps. Do not start a vdC-III campaign here. Best next
question: third-derivative van der Corput on those two
chirps.

## Publication assessment

Status: `EXPLORATORY` (negative knowledge). Not a Paper B
edit. Paper B already refuses densities beyond \(13/16\).
