# Juggler length-7 integer-\(w\) block

Status: **PARK**. Theorem X and density \(57/64\) stay
**CONJECTURE**. The slogan that \(e(\xi w)\) at
\(\xi\asymp n^{45/32}\) sits below the engine line on
X3-runs is **REFUTED**. Paper B stays frozen at
\(13/16\).

Phase-38 desk classification of the Phase-13 integer
block. Child of
[juggler_engine_harvest.md](juggler_engine_harvest.md).
Not a \(K_3\) attack and not a Paper B edit. Not another
rewriting of \(e(uw^{3/2})\).

## Problem

Lemma X1 rearranges the naive supercritical
\(\theta_w\) coefficient \(\tfrac94 v^{5/8}\asymp
n^{45/32}\) into the integer term \(\tfrac94 w\,v^{5/8}\).
Phase 13 claimed this integer block is handled on
Lemma X3 runs by the Q/R3 pattern. Independently of
\(e(uw^{3/2})\), does \(e(\xi w)\) sit below the engine
line, or is it the same Phase-5 wall?

## Exact statement

For \(\xi=\tfrac{9k}4 v^{5/8}\asymp kn^{45/32}\) and
\(1\le\lvert k\rvert\le P^{1/24}\), is \(e(\xi w)\) an
engine or a frozen-frequency chirp on X3-runs (coefficient
\(<n\) and derivative \(<1\), or a \(C^3\) interpolant
already covered by Lemma X5), or does it sit above
Theorem Q’s line \(c'\asymp 1\)?

## Current literature

- Lemma X1 (`sixth_ooeoo_scan`) — **EXACT — HUMAN
  PROOF**. Names the naive coefficient \(n^{45/32}>n\)
  and removes it. **reproduced**.
- Theorem Q engine line — **EXACT — HUMAN PROOF**.
  **reproduced**.
- Lemma X5 (`J-length7-vdc3-chirps`) — **EXACT — HUMAN
  PROOF**. The smooth interpolant \(e(\xi U)\) is the
  first-letter chirp, not this block. **independent**.
- X3 plus Q/R3 for \(e(uw^{3/2})\)
  (`J-length7-x3-qr3-carry`) — **REFUTED**. Same
  exponent \(45/32\), different object. **independent**.
- Phase-5 wall — **REFUTED** as an engine route.
  **reproduced**.
- Theorem X — **CONJECTURE**. **extended** by a named
  method failure.

## Branch budget

```text
Mathematical target     Does e(ξ w), ξ ≍ n^{45/32}, sit
                        below the engine line on X3-runs,
                        or is it the Phase-5 wall?
Novelty hypothesis      Linear e(ξ w) might be a different
                        species from e(u w^{3/2}); X3 could
                        make ξ J a smooth chirp.
Falsifier               ξ > n or ξ' ≫ 1, or the {U}-sawtooth
                        exceeds the engine line, or ξ drifts
                        by ≫ 1 on an X3-run.
Existing machinery      Lemma X1, X3, X5; Theorem Q engine
                        line; Phase 37 carry scan.
Maximum Phase-0 scope   Desk classification of ξ and the
                        three X3 readings. No Theorem X
                        retag, no e(u w^{3/2}) rewrite,
                        no length-8, no Paper B, no new CLI.
Promotion criterion     ξ w is an existing engine/chirp
                        with displayed room inside P^{23/24}.
Stop criterion          Same Phase-5 wall, or a new class
                        is required (PARK X).
```

## Balanced-ternary formulation

None required. The map is on ordinary positive integers.

## Why BT may be relevant

It is not required. The 2-adic / BT bridge stays closed.

## Candidate operations / invariants

- Naive X1 coefficient \(\tfrac94 v^{5/8}\asymp n^{45/32}\)
  — **EXACT — HUMAN PROOF** (already in Lemma X1)
- “Integer-\(w\) sits below the engine line on X3-runs”
  (`J-length7-integer-w-block`) — **REFUTED**
- Isolated \(e(\xi U)\) as Lemma X5 — **EXACT — HUMAN
  PROOF** (not this block)
- Theorem X / density \(57/64\) — **CONJECTURE**
- Length-8 \(E'\) — **CONJECTURE** (untouched)
- \(K_3\) bound — **PARK** (BB/GG/JJ)

## Experiments

No new runner. Exponent seals in
`tests/research/juggler_sequence/test_two_step_parity.py`
(`test_length7_integer_w_engine_line`). Carry geometry
reuses `w_carry_run_scan`.

## Conjectures

None new. `J-depth7-engine-contracting` and
`J-seven-step-descent-density` stay conjectures.

## Counterexamples

None for the counts. The method counterexample is
\(\xi\asymp n^{45/32}>n\) with \(\xi'\asymp n^{13/32}\gg 1\),
which is the coefficient Lemma X1 eliminated.

## Formalization

None added.

## Results

- **Proposition X-int (REFUTED,
  `J-length7-integer-w-block`).**
  \(e(\xi w)\) at \(\xi\asymp n^{45/32}\) does not sit
  below the engine line on X3-runs. It is the Phase-5
  wall. Proof: lemma Part XXII.

## Open questions

The length-8 \(E'\) hole, a different door. Do not
reopen \(e(uw^{3/2})\) and do not rewrite the
\(45/32\) coefficient. The OOOEO sibling at
\(n^{27/32}\) Q-expands to the already-killed
\(n^{27/16}\)-chirps and is not an escape.

## Decision

**PARK** Theorem X. The integer-\(w\) block is the
same Phase-5 wall, independently of \(e(uw^{3/2})\):
it reinstates the naive \(\theta_w\) coefficient
Lemma X1 removed. The existing-toolkit route for the
OOEOO sixth letter is closed. Best next question:
the length-8 \(E'\) hole.

## Publication assessment

Status: `EXPLORATORY` (negative knowledge). Not a
Paper B edit. Paper B already refuses densities
beyond \(13/16\).
