# Juggler harvest counting program

Status: **CLOSE**. Theorems X and AA, and the densities
\(57/64\) and \(29/32\), stay **CONJECTURE**. The slogan
that an outside-toolkit Juggler attack on
\(e(u w^{3/2})\) remains is **REFUTED**. Paper B stays at
the imported length-5 harvest (\(7/8\)).

Phase-41 desk inventory of the Phase-40 leftover. Child of
[juggler_engine_harvest.md](juggler_engine_harvest.md).
Not a \(K_3\) attack and not a Paper B edit. Not a
rewriting of \(e(uw^{3/2})\).

## Problem

Phase 40 closed the length-8 \(E'\) hole and left
Theorem AA blocked only by Theorem X. Is there an
outside-toolkit attack on the X leftover
\(\sum e(u w^{3/2})\), or is the harvest counting
program terminal?

## Exact statement

Does any reading of \(\sum e(u w^{3/2})\) at
\(1\le\lvert u\rvert\le P^{85/96}\) sit outside the
killed toolkit (passenger / decoration / isolated chirp
/ X3–Q/R3 / integer-\(w\)) *and* remain a Juggler
construction with a displayed \(P^{23/24}\) target?

## Current literature

- Passenger slogan (`J-length7-passenger-theorem-t`) —
  **REFUTED**. **reproduced**.
- Isolated chirps (`J-length7-vdc3-chirps`) —
  **EXACT — HUMAN PROOF**. Not the inventory.
  **reproduced**.
- X3 plus Q/R3 (`J-length7-x3-qr3-carry`) —
  **REFUTED**. **reproduced**.
- Integer-\(w\) (`J-length7-integer-w-block`) —
  **REFUTED**. **reproduced**.
- Length-8 remainder (`J-length8-remainder-discard`) —
  **EXACT — HUMAN PROOF**. Consumes X. **independent**.
- Floor-Hardy / exponent-pair leftovers —
  **CLOSE** as Juggler branches; exported at
  [exponent_pair_two_monomial.md](../theory/exponent_pair_two_monomial.md).
  **independent**.
- Theorem X / Theorem AA — **CONJECTURE**. **extended**
  by a named empty door.

## Branch budget

```text
Mathematical target     Does any attack on e(u w^{3/2})
                        sit outside the killed toolkit,
                        or is the harvest counting
                        program terminal?
Novelty hypothesis      A method not already named
                        (not T-passenger, X3/Q/R3,
                        integer-w, isolated-chirp
                        rewrite, decoration) could
                        still reach P^{23/24}.
Falsifier               Every remaining reading is a
                        nearby reformulation of a
                        killed route, or needs a new
                        analytic theory that is not a
                        Juggler construction.
Existing machinery      Lemmas X1/X4/X5; REFUTED
                        passenger / carry / integer-w
                        rows; Paper B 3.3/3.7/3.10;
                        AA2 remainder discard.
Maximum Phase-0 scope   Desk inventory of leftover
                        readings. No Theorem X retag,
                        no e(u w^{3/2}) rewrite, no
                        Paper B, no new CLI, no new
                        sum-estimate lemma.
Promotion criterion     One named outside-toolkit
                        door with a displayed target.
Stop criterion          The leftover is a killed-route
                        rewrite or an external sum
                        (PARK/CLOSE the program).
```

## Balanced-ternary formulation

None required. The map is on ordinary positive integers.

## Why BT may be relevant

It is not required. The 2-adic / BT bridge stays closed.

## Candidate operations / invariants

- Finite laboratory vocabulary (lemma Part XXIV table)
  — **EXACT — HUMAN PROOF** as a classification
- “An outside-toolkit Juggler attack remains”
  (`J-harvest-counting-terminal`) — **REFUTED**
- Theorem X / density \(57/64\) — **CONJECTURE**
- Theorem AA / density \(29/32\) — **CONJECTURE**
- Nested-floor / exponent-pair bound — **CLOSE** as a
  Juggler construction (already exported)
- \(K_3\) bound — **PARK** (BB/GG/JJ; wrong object)

## Experiments

No new runner. Exponent seals in
`tests/research/juggler_sequence/test_two_step_parity.py`
(`test_harvest_counting_terminal`). The leftover
exponents are already sealed by the passenger, carry,
and integer-\(w\) tests.

## Conjectures

None new. `J-depth7-engine-contracting`,
`J-seven-step-descent-density`,
`J-depth8-engine-quartet`, and
`J-eight-step-descent-density` stay conjectures.

## Counterexamples

None for the counts. The method counterexample is the
empty laboratory vocabulary: every named reading is a
killed route or not a Juggler construction.

## Formalization

None added.

## Results

- **Proposition X-term (REFUTED,
  `J-harvest-counting-terminal`).**
  An outside-toolkit Juggler attack on
  \(\sum e(u w^{3/2})\) does not remain. The harvest
  counting program is laboratory-terminal. Proof:
  lemma Part XXIV.

## Open questions

Do not reopen Theorems X/AA, \(e(uw^{3/2})\),
\(45/32\), or \(E'\). The exponent-pair leftover is
**not** a laboratory door (Phase 42): it stays the
boxed pair of
[exponent_pair_two_monomial.md](../theory/exponent_pair_two_monomial.md).

## Decision

**CLOSE** the harvest counting program. The leftover
\(\sum e(u w^{3/2})\) is real and the counts are not
refuted; the executable Juggler vocabulary is empty.
Best next question: the already-exported exponent-pair
leftover, which is not a harvest door.

## Publication assessment

Status: `ARCHIVED` (negative knowledge). Not a Paper B
edit. Paper B already refuses densities beyond \(7/8\).
