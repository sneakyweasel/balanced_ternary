import Problems.Juggler.CycleCore
import Problems.Juggler.CycleExtrema

/-!
# Fixed cycle words (re-export)

`CycleCore` plus `CycleExtrema`. Existing
`import Problems.Juggler.Cycles` compiles both layers.
Declarations live in those files; this barrel does not restate
them. Leftover proofs import `CycleCore` only.
-/
