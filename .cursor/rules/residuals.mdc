---
description: Cubic Newton-stratum research package — compose, do not re-split
globs: src/research/residuals/**/*.py,tests/research/residuals/**/*.py
alwaysApply: false
---

# `research.residuals`

Last promoted mathematical theory. Status `STRUCTURAL`. It does **not** claim a formula for `M_k(x^3)`. The live publication task is the rewrite note, not a new residual census.

Python names must match `formal/BTCalculus/NewtonStratum.lean`:

- `newton_stratum` / `newtonStratum_*`
- `newton_stratum_n2`, `newton_stratum_n1`, `newton_stratum_n21`, `newton_stratum_n0`, `newton_stratum_q`

Compose existing fibre / `N1` / `N0` / `Q` lemmas. Do not add `cubic_deficit_three.py`. Further casework goes in `stratum.py` or a corollary module.

```python
# BAD — core must not grow residual censuses
from bt.calculus.cubic_fibres import fibre_of

# GOOD
from research.residuals.stratum import newton_stratum, newton_stratum_n2
from bt.calculus.cubic import F_k
```

Canonical math: `docs/theory/cubic_newton_stratum.md`. Short extract:
`docs/theory/newton_stratum_note.md`. Ledger id `BTA-x3-stratum`.

The `x^4` visibility gate is `CLOSE`d in `x4_stratum.py`. Do not open
`x^5`, a quartic count, or a fibre taxonomy.
